import React, { useState } from "react";

export const Deletepost = () => {
    const [deletePost, setDeletePost] = useState()
  return <div>
      
      <button onClick={}>Delete</button>
      <button onClick={()=>props.deletePosts(post)}>Delete</button>
  </div>;
};

export default Deletepost;
